(function () {
    "use strict";

    var URL_TEMPLATE = '...';

    twitter.init = function (selector, template) {
        this.$selection = $(selector);
        this.template =
    };

    twitter.getTweets = function (userName, callback) {

    };

    twitter.buildTweets = function (tweets) {

    };

    twitter.displayTweets = function (tweets) {

    };

    twitter.transformDate = function (tweets) {

    };

}(window.twitter = window.twitter || {}, jQuery));
